# .env.example
# This file contains environment variables for the React client.
# Copy it to a file named ".env" and replace placeholder values with real ones.
# IMPORTANT: Variables in React must start with REACT_APP_ to be exposed to the frontend.

# Base URL of your backend API.
# Example (local): http://localhost:5000/api
# Example (production): https://your-deployed-api.onrender.com/api
REACT_APP_API_URL=your_api_url_here
