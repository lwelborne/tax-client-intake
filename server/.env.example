# .env.example
# This file provides sample environment variables for the project.
# Copy it to a new file named ".env" and replace placeholder values with real ones.

# MongoDB connection string.
# Example: mongodb+srv://username:password@cluster0.mongodb.net/myDatabase?retryWrites=true&w=majority
MONGO_URI=your_mongodb_connection_string_here

# Port number for the backend server.
# Default is 5000, but you can change if needed when running multiple apps locally.
PORT=5000
